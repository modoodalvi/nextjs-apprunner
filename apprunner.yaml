version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm install --prefix app
      - npm run build --prefix app
run:
  runtime-version: 16
  command: node app/.next/standalone server.js
  network:
    port: 8000
    env: APP_PORT
  env:
    - name: PORT
      value: 8080